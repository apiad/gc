- Implement and refactor canonical MCTS logic, update UI for exploration feedback, and clean up unused arguments and linting issues.
- Drafted and saved implementation plan for redesigned two-tiered scan heuristics (Trail + Signatures) in `plans/redesign-scan-heuristics.md`.
- Redesigned scan heuristics using two-tiered selection (Trail + Signatures), updated GCTrail schema, and implemented bottom-up trail persistence.
- Renamed CLI to `fsgc`, made `scan` the default command, renamed `trail` to `inspect`, and implemented recursive tree display with `--depth`.
- Fixed E501 line length linting issues in `src/fsgc/__main__.py` and verified all tests pass.
